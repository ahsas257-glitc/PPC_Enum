This directory is intentionally empty right now.

SQL access currently lives in `app/repositories/` so the runtime path stays easy to trace.
Add query modules here only when shared SQL fragments or a dedicated query layer are actually needed.
